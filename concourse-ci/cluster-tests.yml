
platform: linux
image_resource:
  type: docker-image
  source: {repository: ubuntu, tag: 18.04}
inputs:
  - name: IC
  - name: IC_master
run:
  path: bash
  args: [ "IC/concourse-ci/cluster-tests.sh" ]
params:
  SSH_PRIVATE_KEY: ((SSH_PRIVATE_KEY))
